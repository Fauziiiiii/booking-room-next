export type Room = {
    id: string,
    name: string,
    description: string,
    capacity: number,
    status: string,
    floorId: string,
    buildingId: string,
    createdAt: string,
    updatedAt: string
}