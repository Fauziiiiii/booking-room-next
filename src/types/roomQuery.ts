export type RoomQuery = {
    id: string,
    name: string,
    capacity: number,
    status: string,
    floorId: string,
    floorName: string,
    buildingId: string,
    buildingName: string,
    createdAt: string,
    updatedAt: string
}