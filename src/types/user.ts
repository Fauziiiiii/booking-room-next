export type User = {
    id: string
    nrp: number
    userName: string
    email: string
    password: string
    phoneNumber: string
    companyId: string
    buildingId: string
    floorId: string
    createdAt: string
    updatedAt: string
}