export type Company = {
    id: string
    name: string
    description: string
    city: string
    street: string
    postalCode: string
    country: string
    createdAt: string
    updatedAt: string
}